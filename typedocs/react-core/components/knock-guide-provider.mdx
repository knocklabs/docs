A React context provider that manages in-app guide state and provides guide-related functionality to child components.

## Usage

```jsx
import { KnockProvider, KnockGuideProvider } from "@knocklabs/react-core";

function App() {
  return (
    <KnockProvider apiKey="pk_test_..." userId="user-123">
      <KnockGuideProvider
        channelId="in-app-guide"
        readyToTarget={true}
        listenForUpdates={true}
        colorMode="auto"
        trackLocationFromWindow={true}
      >
        <GuideRenderer />
      </KnockGuideProvider>
    </KnockProvider>
  );
}
```

## Props

<Attributes>
  <Attribute
    name="channelId"
    type="string"
    description="The channel ID for the guide channel."
  />
  <Attribute
    name="readyToTarget"
    type="boolean"
    description="Whether the provider is ready to target and display guides."
  />
  <Attribute
    name="listenForUpdates"
    type="boolean"
    description="Whether to listen for real-time guide updates. Defaults to true."
  />
  <Attribute
    name="colorMode"
    type="ColorMode"
    description="The color mode for guide styling (light, dark, or auto)."
  />
  <Attribute
    name="targetParams"
    type="KnockGuideTargetParams"
    description="Additional targeting parameters for guide display conditions."
  />
  <Attribute
    name="trackLocationFromWindow"
    type="boolean"
    description="Whether to automatically track location changes from the browser window."
  />
  <Attribute
    name="orderResolutionDuration"
    type="number"
    description="Duration in milliseconds for resolving guide order conflicts."
  />
  <Attribute
    name="throttleCheckInterval"
    type="number"
    description="Interval in milliseconds for throttling guide checks."
  />
  <Attribute
    name="children"
    type="ReactNode"
    description="Child components that will have access to the guide context."
  />
</Attributes>
