---
title: Triggering workflows via an event
description: Learn how to trigger cross-channel notification workflows in Knock via an event.
tags: ["trigger", "source", "event", "segment", "cdp", "customer data platform"]
section: Send notifications
---

Events from customer data platforms like [Segment](/integrations/sources/segment) or direct HTTP webhook integrations trigger workflows.

Event-triggered workflows decouple notifications from your backend systems—enabling non-developers to configure notifications based on business events in your product.

Event-triggered workflows need at least one configured [Source](/integrations/sources/overview) connected to Knock.

## Configuring an event trigger

Create and manage event triggers in the "Sources" section under "Developers" or directly in the workflow builder's "Trigger" step.

In the Trigger step sidebar with events connected to Knock, select "Event" from the dropdown menu. Choose an event to trigger this workflow and map critical workflow fields to incoming event payload fields.

[Learn more about sources](/integrations/sources/overview)

## Frequently asked questions

<AccordionGroup>
  <Accordion title="Can I trigger a workflow for multiple recipients from a single event?">
    Yes, you can override the default source event trigger behavior to use a
    property that resolves to a list of recipients.
  </Accordion>
  <Accordion title="Can I cancel a workflow run triggered by an event?">
    No, the `cancel` endpoint does not support event-triggered workflow runs.
  </Accordion>
</AccordionGroup>
